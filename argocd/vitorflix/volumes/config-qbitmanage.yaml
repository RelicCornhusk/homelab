apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-qbitmanage
  namespace: vitorflix
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 4Gi
  storageClassName: proxmox-csi