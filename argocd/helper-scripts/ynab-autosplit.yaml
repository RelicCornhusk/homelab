apiVersion: batch/v1
kind: CronJob
metadata:
  labels:
    app: ynab-autosplit
  name: ynab-autosplit
spec:
  schedule: "*/5 * * * *"
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: ynab-autosplit
        spec:
          restartPolicy: OnFailure
          containers:
          - name: ynab-autosplit-vitor
            image: ghcr.io/reliccornhusk/ynab-autosplit:0.0.1
            envFrom:
            - secretRef:
                name: ynab-autosplit-env-vitor
          - name: ynab-autosplit-thais
            image: ghcr.io/reliccornhusk/ynab-autosplit:0.0.1
            envFrom:
            - secretRef:
                name: ynab-autosplit-env-thais

